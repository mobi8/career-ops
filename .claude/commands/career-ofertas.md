---
description: Compatibility alias for career-ops multi-offer comparison
argument-hint: "[offers, URLs, or report references]"
---

Compare the following offers using career-ops `ofertas` mode:

$ARGUMENTS

Load the career-ops skill and route to `ofertas`:

```text
/career-ops ofertas $ARGUMENTS
```
